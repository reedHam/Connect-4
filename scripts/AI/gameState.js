class GameState {
    constructor(move, board, playerTurn, utility){
        this.move = move;
        this.board = board;
        this.playerTurn = playerTurn;
        this.utility = utility;
    }
}